#!/usr/bin/env bash
set -euo pipefail
KULE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PY="$KULE/venv/bin/python"
if [[ ! -x "$PY" ]]; then
  echo "[HATA] Sheriff venv yok. Once: $KULE/venv_kur.sh" >&2
  exit 2
fi
exec "$PY" "$KULE/main.py" "$@"
